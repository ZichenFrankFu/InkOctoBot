[pytest]
testpaths = tests
markers =
    unit: Unit tests (no external services)
    integration: Integration tests (require Ollama or other services)
    skill: Skill tests
    agent: Agent orchestration tests
asyncio_mode = auto
