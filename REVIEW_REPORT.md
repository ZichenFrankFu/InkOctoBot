# InkOctoBot 综合 Review 报告

## 一、功能实现状态总览（排除爬虫相关内容）

### 已完整实现的核心功能

| 模块 | 状态 | 说明 |
|------|------|------|
| 项目管理 CRUD | 完整 | 创建/编辑/删除项目，JSON 持久化 |
| 编辑器（章节文本编辑） | 完整 | 卷/章管理、字数统计、自动保存 |
| 版本管理 | 完整 | 章节版本保存、diff 对比、版本回滚 |
| 角色管理 | 完整 | CRUD + AI 生成人设 + 关系图 + 决策模型(Layer B) |
| 世界书 | 完整 | 词条 CRUD + AI 一致性检查 |
| 剧情线（Storyline） | 完整 | 可视化节点图、拖拽、连线、自动布局 |
| 参考作品库 | 完整 | 作品 CRUD + 上传 + 风格分析 + 学习维度 |
| 设置页 | 完整 | Pipeline 模型分配、供应商管理、系统参数 |
| 模型供应商管理 | 完整 | 多供应商支持 + 连接测试 + Ollama 自动检测 |
| API 用量统计 | 完整 | 按供应商/模型/角色统计，持久化 |
| AI 生成 Pipeline | 完整 | SceneDirector -> ActorAgents -> EditorWriter -> Evaluator 完整链路 |
| 文本评估（Eval） | 完整 | 重复检测、slop检测、风格漂移、质量评分 |
| 公式引擎（Formula） | 完整 | 类型预设 + 多维聚合 + prompt 约束生成 |
| Skill 注册与管理 | 完整 | 技能注册表、Agent 分域、学习日志 |
| 事件总线 + WebSocket | 完整 | 实时事件流、建议推送 |
| 导出功能 | 完整 | 支持 txt/docx/epub 格式 |

### 部分实现 / 有缺口的功能

| 模块 | 问题 | 严重程度 |
|------|------|----------|
| **config_api** | 只有 health 端点，无实际配置管理逻辑 | 低 — settings_api 已覆盖此职责 |
| **tasks_api** | 只有 health 端点，无任务管理功能 | 中 — 缺少后台任务队列/进度追踪 |
| **version_api** | 只有 health 端点，委托给 editor_api | 低 — 可考虑移除冗余 router |
| **security_api** | 只有 health + key-status，缺 key 管理端点 | 低 — API Key 已在 settings 页管理 |
| **CLI (cli.py)** | 多个命令是 stub/TODO（project create/list, generate, analysis trend, memory consolidate 等） | 中 — CLI 与 UI 功能重复，但 headless 使用场景缺失 |
| **generation_api 流式生成** | WebSocket streaming 端点代码存在但前端未充分利用 | 中 — 长文本生成时用户无法看到实时进度 |
| **model cost 估算** | `/api/models/cost/{project_id}` 返回全零 stub | 低 — 功能占位，未接入实际定价 |

### 冗余/可清理的模块

| 模块 | 建议 |
|------|------|
| `config_api.py` | 功能与 settings_api 重叠，建议合并或移除 |
| `version_api.py` | 仅一个 health 端点，逻辑已在 editor_api，建议移除 |
| `tasks_api.py` | 如不打算实现后台任务队列，建议移除 |
| `DevToolsPage.tsx` | 文件存在但已从路由移除，建议删除文件 |
| `database/` 目录 | db_schema.py、db_handler.py、reference_schema.py 全部服务于爬虫数据库，非本项目核心（创作数据用 JSON 文件存储） |

---

## 二、UI/UX 全面评审

### A. 整体架构评价

**导航结构 (App.tsx)**
- 左侧固定侧边栏 + 右侧主内容区，经典 SPA 布局
- 四个分组：概览、数据、创作、系统 — 逻辑清晰
- 侧边栏可拖拽调整宽度 — 细节到位

**设计系统 (global.css)**
- 深色优先设计，配色体系完整（vermillion accent + jade/gold/indigo/purple 辅助色）
- 完整的 CSS 变量体系，typography 层级分明
- 缺少 light mode 实际实现（虽声明了 ThemeMode 类型）

### B. 逐页面评审与改进建议

---

#### 1. 首页 (DashboardPage)

**现状**：分为「我的创作」和「市场数据速览」两个区块。创作区展示项目卡片 + 参考库偏好；市场区展示题材分布/热门标签条形图 + 高频上榜作品表格。

**问题**：
- P1: 「我的创作」与「市场数据」信息密度不均 — 市场数据占据页面 70%+ 空间，但作为「首页」应以创作为主
- P1: 项目卡片点击统一跳转 editor，未设置 activeProject，导致可能打开错误项目
- P2: 「AI 建议」硬编码为「继续创作第 N+1 章」，无实际 AI 逻辑
- P2: 无「今日写作目标」或「写作日历/热力图」等激励性元素
- P3: 市场数据在无爬虫数据库时显示「暂无数据」空状态文案不友好

**改进建议**：
1. 将首页重构为「创作工作台」导向：今日目标、最近编辑章节快速入口、写作统计趋势
2. 市场数据降级为可折叠的辅助区域，或移到单独的「市场」tab
3. 项目卡片点击应先 `onSelectProject(proj.id)`，再跳转
4. 添加「最近活动」时间线（最近保存、最近 AI 生成、最近评估等）

---

#### 2. 开书/项目管理 (ProjectListPage)

**现状**：左侧项目列表 + 右侧「AI 创作工作室」（热点题材 / 头脑风暴 / 风格校准三个 tab）。项目支持网格/列表切换。有完整的创建/编辑/删除流程。

**问题**：
- P1: 页面职责过重 — 同时承载「项目管理」和「AI 头脑风暴」两个完全不同的功能
- P1: 创建项目表单嵌入在页面内，无 modal/dialog 封装，创建时整个页面布局变化
- P2: 风格校准的三个滑块 (tone/pacing/rhetoric) 只存 state，未与 Pipeline 生成设置打通
- P2: 头脑风暴 chat 的 AI 对话历史仅存 sessionStorage，刷新丢失
- P3: 项目卡片缺少最后编辑时间、进度指示等关键信息

**改进建议**：
1. 将「AI 创作工作室」独立为单独页面或将头脑风暴整合进编辑器侧边栏
2. 项目创建改为 modal dialog，减少页面跳动
3. 校准参数持久化到项目设置，并在 Pipeline 生成时注入
4. Chat 历史持久化到后端 (chat_history API 已存在但未充分利用)

---

#### 3. 编辑器 (EditorPage)

**现状**：左侧章节树 + 中间文本编辑器 + 右侧 AI 面板。支持 Pipeline 生成、版本对比、文本评估、AI 改写。有完整的 Pipeline 可视化（4 步骤 Agent 对话流）。

**问题**：
- P1: textarea 作为编辑器，缺少基本的富文本/markdown 支持（加粗、斜体、段落样式等）
- P1: 无「撤销/重做」按钮，依赖浏览器原生 Ctrl+Z，且跨版本恢复困难
- P2: AI 面板占据固定右侧空间，在小屏幕上压缩编辑区域
- P2: Pipeline 执行时无法中途编辑已生成的内容（需等待完成）
- P2: 缺少字数目标/进度条（如「本章目标 3000 字，已写 1200 字」）
- P3: 版本对比 diff 视图功能完整但入口不够直观
- P3: 无「专注模式」（全屏写作，隐藏所有面板）

**改进建议**：
1. 添加 Zen Mode / 专注模式（快捷键 F11 或按钮切换）
2. 添加章节字数目标进度条
3. AI 面板改为可收起的抽屉式，默认收起
4. 考虑引入轻量级富文本编辑器（如 TipTap 或保持纯文本但添加 Markdown 预览）
5. 添加「插入 AI 内容到光标位置」功能而非替换全文

---

#### 4. 角色管理 (CharacterManagerPage)

**现状**：左侧角色列表 + 右侧详情/关系图切换。支持 AI 生成人设、关系图可视化、决策模型 Layer B 参数、动态属性快照。

**问题**：
- P2: 角色卡片信息密度高，详情编辑需滚动大量内容
- P2: 关系图 (RelationshipGraph) 在角色少时体验好，角色多时节点重叠
- P2: AI Chat 生成人设的对话流程较长，用户需多轮对话才能得到满意结果
- P3: Layer B 决策模型参数过于专业，普通用户难以理解参数含义
- P3: 批量操作模式存在但功能有限（仅删除）

**改进建议**：
1. 角色详情改为 Tab 式布局（基本信息 / 性格与背景 / 关系 / 决策模型）
2. Layer B 参数添加更直观的解释和预览（如「冒险程度」滑块 + 情景示例）
3. 添加「角色对比」功能，方便查看多角色差异
4. AI 生成人设添加「一键生成」模式（基于名字+角色定位直接生成完整档案）

---

#### 5. 世界书 (WorldBookPage)

**现状**：词条列表 + 编辑器 + AI 一致性检查。支持分类、标签过滤。

**问题**：
- P2: 词条之间缺少交叉引用/链接
- P2: 一致性检查结果展示较为原始（纯文本）
- P3: 无搜索功能或搜索入口不明显
- P3: 词条编辑器与角色管理的世界观信息有重叠

**改进建议**：
1. 添加词条间 `[[引用]]` 语法和自动链接
2. 一致性检查结果结构化展示（矛盾点 + 修复建议 + 一键修复）
3. 添加全局搜索框，支持跨词条内容搜索
4. 考虑与角色管理合并「世界观设定」相关内容

---

#### 6. 剧情线 (StorylinePage)

**现状**：自定义 canvas 实现的节点图，支持拖拽、连线、自动布局。节点包含章节号、时间、地点、角色等信息。有时间线和周次视图。

**问题**：
- P1: 自定义 canvas 实现性能和交互有限（无缩放、无小地图、无组合选择）
- P2: 连线操作不够直观（需要先点击「连线」按钮进入连线模式）
- P2: 节点编辑面板信息过多，混合了元数据和内容
- P3: 无法从剧情线直接跳转到对应章节编辑

**改进建议**：
1. 添加画布缩放和平移（鼠标滚轮 + 拖拽空白区域）
2. 添加从剧情节点到编辑器章节的快捷跳转
3. 支持节点分组（按「卷」聚合）
4. 考虑集成 React Flow 等成熟库替代自定义实现

---

#### 7. 参考作品库 (ReferenceLibraryPage)

**现状**：作品卡片网格 + 详情侧边栏 + 条目编辑器。支持上传、风格雷达图、学习维度管理。

**问题**：
- P2: 上传流程需要手动填写大量元数据
- P2: 风格雷达图 (StyleRadar) 数据来源不清晰
- P3: 缺少批量导入功能
- P3: 作品与项目的关联不够直观

**改进建议**：
1. 上传后 AI 自动提取元数据（标题、作者、类型等）
2. 添加「关联到项目」的显式操作按钮
3. 添加阅读笔记/批注功能，与风格学习深度整合

---

#### 8. 设置页 (SettingsPage)

**现状**：三 Tab 结构（Pipeline 配置 / 模型供应商 / 系统设置）。功能全面。

**问题**：
- P2: Pipeline 配置中供应商和模型是分开的下拉框，但模型列表依赖先选供应商，交互不流畅
- P2: 供应商卡片信息密度高，API Key 输入框无「显示/隐藏」toggle
- P3: 系统设置中「清除缓存」按钮仅清 localStorage，实际效果有限
- P3: 缺少「导入/导出设置」功能

**改进建议**：
1. API Key 输入添加「显示」toggle 图标
2. Pipeline 配置添加「一键全部使用同一模型」快捷操作
3. 添加设置导入/导出功能（方便多设备同步）
4. 添加连接状态实时指示器（绿色/红色圆点在供应商卡片上）

---

#### 9. 市场数据库 (RankingsPage)

**现状**：三级钻探式导航（榜单列表 -> 快照 -> 条目），支持作品详情侧边栏。

**评价**：功能完整，交互清晰。依赖爬虫数据库。作为数据浏览器已足够好。

---

#### 10. 分析面板 (AnalysisDashboardPage)

**现状**：趋势分析 + 标签热力 + 题材机会 + 共现分析。依赖爬虫数据库。

**评价**：数据可视化丰富，功能完整。依赖爬虫数据库。

---

#### 11. 智能体 (SkillsPage)

**现状**：Agent 分域展示 + Skill 注册表 + 学习日志。

**问题**：
- P2: 技能列表主要展示内部注册信息，对用户不够友好
- P3: 学习日志缺少可视化趋势

**改进建议**：
1. 将此页面重新定位为「AI 助手能力面板」，展示各 Agent 的能力、状态和使用统计
2. 学习日志添加时间线可视化

---

### C. 跨页面通用问题

| 编号 | 问题 | 优先级 |
|------|------|--------|
| G1 | **无全局搜索** — 无法快速搜索角色、世界书词条、章节内容 | P1 |
| G2 | **无键盘快捷键** — 专业创作工具应有丰富的快捷键支持 | P1 |
| G3 | **无面包屑/路由历史** — 用户无法回到上一个页面状态 | P2 |
| G4 | **无 Toast/通知系统** — 操作反馈依赖各页面自行实现 | P2 |
| G5 | **无 Loading skeleton** — 加载状态统一使用 spinner，可改为 skeleton 减少布局跳动 | P3 |
| G6 | **无响应式设计** — 固定宽度布局，在小屏幕上体验差 | P2 |
| G7 | **无 Light Mode** — theme.ts 声明了类型但无实际实现 | P3 |
| G8 | **内联 style 过多** — 大量组件使用 `style={{}}` 而非 CSS class，维护困难 | P3 |
| G9 | **无错误边界 (Error Boundary)** — 单个组件崩溃可能导致整个应用白屏 | P2 |
| G10 | **无 onboarding/引导流程** — 新用户首次使用无指引 | P2 |

---

## 三、优先级排序的改进路线图

### Phase 1: 核心体验提升（高影响 + 低成本）

1. **全局快捷键系统** — Cmd/Ctrl+K 搜索、Cmd+S 保存、F11 专注模式
2. **编辑器专注模式** — 隐藏侧边栏和 AI 面板的全屏写作模式
3. **项目卡片点击修复** — 点击先切换 activeProject
4. **统一 Toast 通知** — 提取为全局组件
5. **Error Boundary** — 防止白屏崩溃
6. **AI 面板可收起** — 编辑器右侧面板改为 collapsible

### Phase 2: 创作流程优化（高影响 + 中成本）

7. **首页重构** — 以创作为中心，添加今日目标和最近活动
8. **章节字数目标** — 编辑器添加进度条
9. **项目创建 Modal** — 独立 dialog 替代内嵌表单
10. **校准参数持久化** — 风格偏好存入项目配置
11. **角色详情 Tab 化** — 减少滚动，信息分组更清晰

### Phase 3: 专业功能增强（中影响 + 高成本）

12. **全局搜索** — Cmd+K 调出搜索面板
13. **响应式布局** — 支持平板和小屏幕
14. **Light Mode** — 完整明亮主题
15. **剧情线升级** — 缩放、小地图、章节跳转
16. **世界书交叉引用** — `[[链接]]` 语法

### Phase 4: 清理与优化

17. 移除冗余 router（config_api, version_api, tasks_api）
18. 删除 DevToolsPage.tsx 文件
19. 内联 style 迁移到 CSS class
20. CLI stub 命令要么实现要么移除
