# core crawling
requests>=2.28.0
beautifulsoup4>=4.11.0
lxml>=4.9.0

# browser automation
selenium>=4.15.0
webdriver-manager>=4.0.0
undetected-chromedriver>=3.5.5

# data & db
pandas>=1.5.0
numpy>=1.24.0
sqlalchemy>=1.4.0
PyYAML>=6.0

# scheduling
schedule>=1.2.0

# analysis & visualization
matplotlib>=3.7.0
seaborn>=0.12.0
wordcloud>=1.9.0
jieba>=0.42.0
openpyxl>=3.1

# utils
python-dateutil>=2.8.2
tqdm>=4.66.0
fonttools>=4.61.1
pywebview>=6.1

# feature extraction & clustering
scikit-learn>=1.3.0

# 语言学文本特征 — 可选增强（全部缺失时自动降级，见 ner_backend.py / sentiment.py /
# linguistics.py）。词性分布 / 词汇丰富度 / DUTIR 词典情感 / 人名库种子 仅依赖 jieba，
# 无需以下任何包即可工作；以下仅用于「升级」到深度/句法路径：
#   ltp>=4.2.0          # 人名 NER（GPU→CPU 自动）+ 依存分析（MDD 真值）
#   torch>=2.0.0        # LTP 后端 + 预留的本地 BERT/RoBERTa/ERNIE 情感模型
# psutil 用于硬件检测（内存/算力门槛）；缺失时按保守默认值，不影响降级正确性。
psutil>=5.9.0

# embedding stack（本地 sentence-transformers 后端）。
# transformers 4.41 起新增 EncoderDecoderCache；低于 4.41 时
# sentence-transformers ≥ 3 的 trainer 导入链会抛
# `cannot import name 'EncoderDecoderCache' from 'transformers'`，
# chromadb_indexer 会自动降级为跳过本章索引（参见
# ui/backend/app/services/embedding/service.py:EmbeddingBackendUnavailable）。
# 锁版本以阻断升级方向上的回退。注意：与 name_pretrainer/requirements.txt
# 的 `transformers>=4.26,<4.41`（LTP 4.2.x 兼容）冲突 —— name_pretrainer
# 走独立 venv，不要装到同一个环境里。
# CVE-2025-32434: transformers 在 torch < 2.6 时不允许加载 .bin 权重
# (torch.load 漏洞), sentence-transformers 调用链会抛
# "we now require users to upgrade torch to at least v2.6". 即使后端有
# safetensors 也得 torch>=2.6 才能跳过校验, 显式锁版.
torch>=2.6
transformers>=4.41,<5
sentence-transformers>=2.7,<4

# token counting for builder diagnostics (CJK heuristic fallback exists)
tiktoken>=0.5.0

# CLI
typer>=0.9.0

# skill hot-reload (optional)
watchdog>=3.0.0

# testing
pytest>=7.4.0
pytest-asyncio>=0.21.0

# FastAPI backend
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic>=2.9.0,<3.0.0
pydantic-settings>=2.4.0
python-multipart==0.0.9

# LLM providers
openai>=1.12.0
httpx>=0.27.0

# vector store
chromadb>=0.4.0



